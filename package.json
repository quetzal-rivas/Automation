{
  "name": "@aztecgod/ide-directive-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for polling an AWS directive middleware by agent identity.",
  "type": "module",
  "bin": {
    "ide-directive-mcp-server": "./bin/ide-directive-mcp-server.js"
  },
  "files": [
    "bin",
    "src",
    "README.md"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node ./bin/ide-directive-mcp-server.js",
    "check": "node --check ./bin/ide-directive-mcp-server.js ./src/*.js ./test/*.test.js",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  }
}
